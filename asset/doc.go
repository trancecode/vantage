// Package asset provides engine-bundled assets and asset loading. It embeds
// the engine's two default fonts — Google Sans Flex (proportional) and Google
// Sans Code (monospace), both under the SIL Open Font License — and exposes
// helpers to load fonts and images supplied by a game (LoadFont, LoadImage,
// and the path-keyed ImageCache).
package asset
