name: Check Go Version

on:
  schedule:
    # Run every Monday at 9 AM UTC
    - cron: '0 9 * * 1'
  workflow_dispatch:  # Allow manual triggering

permissions:
  contents: read
  issues: write

jobs:
  check:
    uses: trancecode/github-workflows/.github/workflows/check-go-version.yml@main
