name: Auto-Label Claude PRs

on:
  pull_request:
    types: [opened, reopened, synchronize]
  workflow_dispatch:

permissions:
  contents: read
  pull-requests: write
  actions: read

jobs:
  auto-label:
    uses: trancecode/github-workflows/.github/workflows/auto-claude-label.yml@main
