package util

// DebugMode indicates whether debug mode is enabled. It gates the engine's
// diagnostics (the screen logger, the profiler, the watchdog) and is toggled at
// runtime by the app's debug key.
var DebugMode = true
